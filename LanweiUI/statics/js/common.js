(function (window) {
    if (window.console === undefined) {
        window.console = {
            log: function () {
            }, info: function () {
            }, debug: function () {
            }
        };
    }
})(window);
